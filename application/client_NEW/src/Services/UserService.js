export default {

    // //Fetch a users data (non-sensitive - username, ID and role)
    // getUserData: userID => {
    //     return fetch(`/api/user/user-details/${userID}`)
    //         .then(res => {
    //             return res.json().then(data => data)
    //         });
    // }

}